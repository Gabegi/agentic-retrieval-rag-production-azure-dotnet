# AgenticRagApp.Querying

Query-time pipeline behind `POST /api/query`: take a Dutch question, retrieve through the Azure
AI Search **knowledge base**, and return a cited answer.

The answer is **not** generated by a chat call in this project. `KnowledgeService`
(`AgenticRagApp.Infrastructure`) configures the knowledge base with retrieval and answer
instructions; the knowledge base decomposes the question into sub-queries, searches the index
and synthesizes the answer. This project orchestrates that call and post-processes the result.

```
Services/
  AgenticRagQueryService.cs        IRagQueryService — the flow below
  ChunkNeighborExpander.cs         fetches previous/next-page chunks of every hit via a raw SearchClient side-channel,
                                   rebuilds reading order, caps context at 16,000 chars
  KnowledgeBaseReferenceMapper.cs  knowledge-base references → RetrievedChunk / Citation ([Title] - p.N)
  KnowledgeBaseActivitySummary.cs  sub-query count, sub-queries, token accounting off the retrieval activity log
  ContextTokenEstimator.cs         chars/3.1 estimate of the context handed to the model; shared with the eval harness
  Interfaces/IRagQueryService.cs
Guards/
  PromptInjectionGuard.cs          IPromptInjectionGuard — local regex short-circuit, then Prompt Shields (IPromptShieldClient),
                                   also over retrieved text for indirect injection
  PiiGuard.cs                      IPiiGuard — BSN (elfproef) + Dutch postcode regexes, then Azure AI Language PII detection
  NumericGroundingGuard.cs         static — numeric claims in the answer that appear nowhere in the retrieved context
                                   (detection only; logged here, scored per scenario by the eval)
Models/                            RagQueryResult, RetrievedChunk, Citation
ServiceCollectionExtensions.cs     AddQuerying()
```

## Flow (`AgenticRagQueryService.AskAsync`)

1. Prompt-injection guard on the question.
2. PII guard on the question.
3. `IKnowledgeRetrievalClient.RetrieveAsync` — the knowledge base searches and synthesizes.
4. Map references to chunks and citations; expand neighbouring pages; estimate context tokens.
5. PII guard on the answer; numeric-grounding check; prompt-injection guard over retrieved text.
6. Return `RagQueryResult`; `QueryingFunction` writes a `QueryRunReport`.

## Guard enforcement mode — read this

Acceptance criteria 4 (prompt injection) and 5 (no personal data in question or answer) exist
because model instructions can be bypassed, so the guards above are the only thing that enforces
them. **They currently do not block.** `IndexerConfig.GuardsLogOnly` is `true`: a guard that
fires logs a warning and the knowledge base's answer is returned anyway. The fixed Dutch fallback
texts (`PiiFallback`, `InjectionFallback`) are only returned when it is `false`.

The property's comment says `GUARDS_LOG_ONLY=false` restores enforcement without a code change,
but the setting is **not read from configuration** — `GuardsLogOnly` is absent from the
`IndexerConfig` initializer in `Infrastructure/Clients/ServiceCollectionExtensions.cs`. Flipping
it is a code change until that is wired. Set 2026-08-12 by request pending eval evidence of how
often each guard fires: `docs/2608/260812/guards-review.md` (D091), `docs/2608/260814/guards-notes.md` (D108).

## Acceptance criteria

[AcceptatieCriteria.md](AcceptatieCriteria.md) — the seven business criteria, which are met by
knowledge-base instructions (1, 2, 3, 6, wording of 7) and which need code (4, 5), with the
change plan and its status. B1-level Dutch (criterion 1, changed from C1 on 2026-08-12) still
needs PO sign-off.

## Configuration

Everything `IndexerConfig` requires, notably `CONTENT_SAFETY_ENDPOINT` and `LANGUAGE_ENDPOINT`
(both on the shared Foundry account) — table in [RunningLocally.md](../../RunningLocally.md#configuration).

## Tests

`src/UnitTests/AgenticRagApp.Querying.Tests` (MSTest, 8 files). `AgenticRagQueryServiceTests`
pins both `GuardsLogOnly` modes.

## See also

- `docs/2608/260827/golden-questions-agentic-redesign.md` (D172) — the golden set is built around retrieval capability; `SubQueryCount` / `DistinctDocumentsCited` come from `KnowledgeBaseActivitySummary`
- `docs/2607/260730/query-performance.md` (D030) — open question on burst load against the query deployment
- [Reports.md](../AgenticRagApp.Observability/Reports.md) — where the per-query report lands
