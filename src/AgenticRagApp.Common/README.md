# AgenticRagApp.Common

Shared models referenced by other `src/` projects — no logic, no dependencies on other projects here.

- `Models/` — plain data contracts (DTOs) shared across indexing, querying, and infrastructure
